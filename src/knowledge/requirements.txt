fastapi==0.109.0
uvicorn[standard]==0.27.0
google-cloud-aiplatform[generative-ai]==1.74.0
# google-cloud-firestore removed: no longer needed after PageIndex migration.
# Restore when vector-based cache layer is added (see tools/archive/create_cache.py).
# httpx pinned: 0.28+ breaks starlette TestClient (starlette 0.35 compat)
httpx==0.27.2
